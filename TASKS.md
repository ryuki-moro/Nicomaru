# にこまる — タスク一覧

基本設計書 **Version 1.2**（2026-08-28）時点の残タスク。

- v1.1（2026-07-28）でレビュー指摘55件を本文へ反映。
- v1.2（2026-08-28）で **Phase 1 着手ブロッカーを全件解消**。内訳は下記のとおり。

## 1. Phase 1 着手ブロッカー — 解消済み

rank 1〜10 について v1.1 本文を原文照合で検証した（観点別10体の並列レビュー＋「未解消」判定への反証検証）。

| rank | 検証結果 | v1.2 での対応 |
| --- | --- | --- |
| 1 | v1.1 で解消済み（付録A の共通関数群が正） | 6-3-4 に残っていた v1.0 の再帰SQL例・旧関数名（`my_case_ids` / `is_planner_of`）を付録A へ一本化。`submit_task()` と couple 向け update ポリシーを付録A に追加 |
| 2 | **未解消**（主連絡先の決定規則が無い／couple_profiles を couple が改変できる） | K03 に「主連絡先」を追加。`is_primary_contact` と `recipient_email` の割当規則を 6-6-1 に明記。氏名の正本を確定（P02 入力は `display_name` のみ）。付録A で couple_profiles の書き込みを planner／admin に限定 |
| 3 | v1.1 で解消済み | ─ |
| 4 | **未解消**（case_invitations の RLS が無い／平文トークンを再送できない） | 付録A に case_invitations のポリシーと部分ユニークインデックスを追加。`GET /invitations` を追加し、送信は必ず再発行を伴う仕様に確定。`sent_at` を追加。表8 の 1-1 を K03, K02 に修正 |
| 5 | v1.1 で解消済み | `user_profiles_admin_write` の `with check` を U02 の規則に合わせて `role = 'admin'` へ狭めた |
| 6 | **未解消**（memo が couple に素通し／couple_profiles が for all） | memo を列レベル権限で `authenticated` から剥奪し、`get_couple_memo()` 経由に変更。書き込みポリシーを staff 限定へ分割 |
| 7 | **未解消**（`submission_format='none'` の完了手順が無い／表番号の誤り） | `submission_type` の値域を4値へ統一し、none は「確認しました」で提出1行を作る仕様に確定。表6-5→表6-9 の誤参照を修正。K02「宿題を追加」の入力項目を定義 |
| 8 | **未解消**（パスワード最小長が2値／invited→active の経路が無い） | P03 を12文字以上に統一。`/api/auth/complete-invite` を新設し invited→active の遷移を確定。初回設定リンク（generateLink、72時間、着地は P03 ステップ2）を明記。表56 に `/api/venues` を追加 |
| 9 | v1.1 で解消済み | ─ |
| 10 | v1.1 で解消済み | ─ |

## 2. 第13章 合意必須事項 — Phase 1 分は決定済み

設計書 **13-1「開発チーム決定（2026年8月28日）」** を新設し、Phase 1 に関わる合意必須事項8件を
開発チームの正式決定として確定した。各行に決定理由と**差し戻し時の影響範囲**を明記してある。

| 論点 | 決定 |
| --- | --- |
| ワンタイム認証の正式採用 | 採用（couple はパスワードレス、staff は12文字以上のパスワード） |
| 招待トークンの有効期限 | initial_registration = 14日 / max_uses 1、mypage_access = 30日 / max_uses 5 |
| 招待トークンと登録メールの結び付き | recipient_email 一致を必須。NULL の招待は確認コード検証を経る |
| 個人情報の暗号化 | アプリ側 AES-256-GCM ＋ 検索用 HMAC-SHA256 列 |
| 案件終了後の自動削除 | archived_at から180日（監査ログのみ2年） |
| ゲスト情報の代理入力 | 同意文面必須、保持は180日 |
| セッション有効期限・失効 | Auth 既定リフレッシュ ＋ `user_profiles.status` によるDB層遮断 |
| 認証メールの送信ドメイン | 開発チームが独自ドメインを取得。Phase 1 は開発者宛送信で検証 |

Phase 3／Phase 3拡張が期限の合意必須4件（AIジョブログ保持・LLMモデル選定・FAQ一次回答の採用可否・
venue_knowledge の投入担当）は、Phase 1 の着手判断に影響しないため既定値のまま進行する。

## 3. 実装

- 🚧 **Phase 1 実装**（案件登録 → 招待 → 初回登録 → 提出 → 確認） — Epic。着手ブロッカーは解消済み
- ⬜ Phase 2（運用機能の拡充：LINE起点導線・リスク可視化・通知・準備シート・system_admin画面）
- ⬜ Phase 3（AI補助 9-1〜9-6）／Phase 3拡張（9-7〜9-11）

### Phase 1 の実装単位

| # | 単位 | 主な設計参照 |
| --- | --- | --- |
| 1 | DBマイグレーション（26テーブル・共通関数・RLS・インデックス・seed） | 5-3 / 6-4 / 付録A / 12章 |
| 2 | 定数・型の単一ソース化（表6-9 の DB値↔UI表示名、提出形式の値域、zod スキーマ） | 12-2 / 表6-9 |
| 3 | 認証（P01 ワンタイム／パスワード、P02 初回登録、P03 再設定・初回設定、complete-invite） | 6-3-1 / 6-6-1 |
| 4 | 案件管理（K01〜K05、case_code 採番、assign-tasks、タイムライン逆算、招待発行・送信） | 5-7 / 6-6-2 / 表58 |
| 5 | マイページ（M01〜M04、提出・一時保存・冪等性） | 6-7 / 表6-9 |
| 6 | プランナー（D01・D02・D04、提出物確認、フォロー記録） | 6-7 |
| 7 | 管理（T01〜T03 テンプレート、U01〜U04 利用者） | 表19・表20 |
| 8 | 検証（RLSテスト・ユニットテスト・E2E・CI） | 11章 / 12-2 |

## 4. レビュー指摘 全55件（v1.1 で反映済み・参照用）

重要 25 / 軽微 30。検証で棄却された27件は除外済み。詳細は `docs/BridalHub_基本設計書_レビュー結果.md`。
rank 1〜10 の検証結果は本書 1章のとおり。rank 11 以降は v1.1 の反映内容をもって解消とする。
