# にこまる — タスク一覧

基本設計書 **Version 1.4**（2026-08-28）、**Phase 1〜Phase 3 完了**時点（Phase 3拡張は未着手）。

## 1. 進捗

| フェーズ | 状態 |
| --- | --- |
| 基本設計 | ✅ Version 1.4 |
| Phase 1（基盤・コア導線） | ✅ 完了 |
| Phase 2（運用機能） | ✅ 完了 |
| Phase 3（AI補助 9-1〜9-6） | ✅ 完了 |
| Phase 3拡張（9-7〜9-11） | ⬜ 未着手（条件付きスコープ。着手条件は実証運用の開始。1-3／7-5） |
| Phase 4（検証） | ⬜ 設計対象外 |

テスト **327件**（PGlite 上のRLS/DBテスト＋純関数のユニットテスト＋実PostgreSQL の並行性テスト10件）。
lint・型チェック・ビルドとも通過。

## 2. 完了したこと

### 設計

- rank 1〜10 を原文照合で検証。rank 2／4／6／7／8 の欠落を **v1.2** で解消
- 第13章の Phase 1 合意必須8件を **13-1「開発チーム決定」**として確定（差し戻し影響範囲つき）
- Phase 2／3 の実装で確定した補助テーブル5件・内部API 8件を **v1.3** で本文へ反映
- Phase 3 の実装で確定した ai_jobs.reviewed_output・ai_worker_heartbeats・API2件と、
  第13章の Phase 3 合意必須2件（AIジョブログの保持・ローカルLLMモデル選定）を **v1.4** で確定

### Phase 1（コア導線）

案件登録 → 招待 → 初回登録 → 提出 → 確認。P01〜P04／M01〜M04／K01〜K05／D01・D02・D04／
T01〜T03／U01〜U04。26テーブル・RLS・業務RPC。

### Phase 2（運用機能）

| 機能 | 内容 |
| --- | --- |
| リスク可視化（6-1〜6-3・4-2） | 算出結果の保存・明示再計算・日次の定期処理・D01/K01/K02 の表示。スコアは必ず根拠とセットで出す |
| 通知（7-1〜7-3・M05・N01） | 付録D の文面テンプレート、LINE送信上限の原子的な確保、上限到達時のメール切替、送信ログ |
| 定期処理（6-12） | pg_cron ＋ pg_net で5処理を登録。死活監視だけ GitHub Actions から外部実行 |
| 準備シート（4-5・D03） | 印刷用CSS（6-11 の第一手段）。生成内容を meeting_sheets へ記録 |
| system_admin（8-1〜8-5・S01〜S03） | 式場管理、利用状況・通知ログ、CSV出力（9章の数式インジェクション対策つき） |
| LINE連携（1-3・M06） | linkToken 方式の4手順、raw body に対する署名検証、重複配信の排除 |

### Phase 3（AI補助）

- **ジョブキュー**（9-6）: `claim_ai_job`（for update skip locked のプル型）／`complete_ai_job`／
  `reclaim_stalled_ai_jobs`／`enqueue_ai_job`／`review_ai_job`
- **最小権限**: ワーカーには Service Role Key を配らず、2関数だけを `ai_worker` ロールへ付与。
  `ai_jobs` を直接 select すらできない（7-3）
- **入出力スキーマ**（7-2）: job_type ごとに zod で定義した単一ソース。
  ワーカーは検証を通してからでないと保存できない
- **9-4 ①ルールベース**: 必須列の欠損・重複・形式違反・行数を決定的に判定。
  LLM が止まっていても指摘が届くようアプリ側に置いた
- **プロンプト**: `ai_prompt_templates` で DB 管理（7-6）。コードに埋め込まない
- **ワーカー**: `worker/ai-worker.ts`（ポーリング30秒、多重起動可、心拍つき）

**実機で通し確認済み**: ローカル PostgreSQL 17.6 ＋ Ollama（gemma3:12b）で
投入 → claim → Ollama → zod 検証 → 書き戻しの全経路が通ることを確認。

#### コア機能の画面（9-1〜9-5）

| 機能 | 画面 | 実装 |
| --- | --- | --- |
| 9-1 分類 | D02／K02 | 提出時にサーバー側から自動投入。D02 で修正して採用（`reviewed_output`）、K02 の宿題一覧に表示 |
| 9-2 準備シート下書き | D03 | 依頼はサーバーアクション（LLMへ渡す内容をサーバーで組み立てる）。採用したものだけ印刷対象に載る |
| 9-3 文面下書き | D02 | 確認フォーム内。コメント欄に入れるだけで送信はしない |
| 9-4 不備一次チェック | D02 | ①ルールベースは描画のたびに毎回。②LLM は専用APIから依頼（CSVの取得・列の間引き・打ち切りをサーバー側で行う） |
| 9-5 宿題起票案 | D05 | 打ち合わせ記録の保存を契機に投入。案は編集可能なフォームとして提示し、登録は既存の宿題追加APIを通す |

- **「利用不可」表示**（7-1／7-3(4)）: ワーカーの心拍（`ai_worker_heartbeats`）が
  10分途切れたら依頼ボタンを出さず、手動運用の案内に切り替える
- **保持期間**（7-4／13-1）: 入出力は完了から30日、行は作成から90日。
  日次の案件自動削除に相乗りする

## 3. 残タスク

### Phase 3拡張（条件付きスコープ）

1-3 のとおり「基盤（9-6）とコア機能（9-1〜9-5）が動作し、実証運用を開始できている場合に限り着手」。

- ⬜ 9-7 FAQ一次回答（pgvector ＋ venue_knowledge。採用可否は第13章の合意必須。既定は「採用しない」）
- ⬜ 9-8〜9-11（リスケ案・引き継ぎサマリー・テンプレート案・翻訳）
- ⬜ venue_knowledge の投入担当（第13章の合意必須。実証運用の開始まで保留）

### 環境（コードでは完結しないもの）

- ⬜ **実環境での通し確認**（#14） — Supabase プロジェクトの作成、Auth メールテンプレートへの
  `{{ .Token }}` 追加、Custom SMTP、Leaked Password Protection、Storage bucket
- ⬜ 認証メール用の独自ドメイン取得と SPF／DKIM（#17）
- ⬜ pg_cron の `app.base_url` / `app.internal_cron_secret` 設定
- ⬜ LINE Messaging API のチャネル作成と Webhook URL 登録
- ⬜ ワーカー用 `ai_worker` ロールのログイン権限付与（Supabase 側）
- ⬜ ワーカー稼働機での `gemma3:12b` の取得（`ollama pull gemma3:12b`）

### そのほか

- ⬜ ワンタイムコードの「検証失敗5回で失効」（#15）
- ⬜ 提出・確認の単一トランザクション化（#16）
- ⬜ 5人並列実装で残った重複と規約の不統一（#18）

## 4. 実装で踏みやすい罠

- **RLS の共通関数は再帰回避のために必須**。ポリシー式から `user_profiles` /
  `couple_profiles` / `wedding_cases` を直接参照すると 42P17 で主要画面が落ちる。
- **permissive ポリシーの WITH CHECK は OR 結合される**。値域だけの WITH CHECK は
  別ポリシーの USING と組み合わさって権限昇格になる。
- **`couple_profiles` は `select *` が 42501**。`COUPLE_PROFILE_COLUMNS` を使う。
- **提出は案件単位**。新郎新婦は同じ宿題に1つの提出を持つ。提出者一致を条件に加えない。
- **後から作った表は grant を個別に付ける**。`grant ... on all tables` は実行時点の表にしか効かない。
- **`RETURNS TABLE` の出力列は表の列と衝突する**。代入の右辺は表名で修飾し、varchar は text へキャストする。
- **`/api/internal/**` と `/api/health` は middleware の除外対象**。除外しないと定期処理が 307 で全滅する。
- **平文の招待トークン・nonce は保存しない**。送信・URL再表示は必ず再発行を伴う。
- **Service Role Key は使用範囲表（6-3-5 表6-4）にある用途のみ**。範囲外は ESLint が落とす。
  用途名は使い回さない。名前が合っていないと「どの行で許可された使用か」を追えなくなる。
- **`RETURNS TABLE` の関数に既定値つき引数を足すと多重定義になる**。
  2引数の呼び出しが 42725 で曖昧になるので、いったん drop してから作り直す。

## 5. レビュー指摘 全55件（v1.1 で反映済み・参照用）

詳細は `docs/BridalHub_基本設計書_レビュー結果.md`。
rank 1〜10 の検証結果は本書の履歴を参照。rank 11 以降は v1.1 の反映内容をもって解消とする。
