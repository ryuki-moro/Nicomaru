# にこまる — タスク一覧

基本設計書 **Version 1.2**（2026-08-28）、**Phase 1 実装完了**時点の残タスク。

## 1. 完了したこと

### 設計 — Phase 1 着手ブロッカーの解消

rank 1〜10 を原文照合で検証した（観点別10体の並列レビュー＋「未解消」判定への反証検証）。
rank 1／3／5／9／10 は v1.1 で解消済み、**rank 2／4／6／7／8 に実装を止める欠落が残っていた**ため v1.2 で修正。

| rank | v1.2 での対応 |
| --- | --- |
| 1 | 6-3-4 に残っていた v1.0 の再帰SQL例・旧関数名を付録A へ一本化。未定義だった `submit_task()` と couple 向け update ポリシーを追加 |
| 2 | K03 に「主連絡先」を新設。`is_primary_contact` と `recipient_email` の割当規則を確定。P02 の氏名は `display_name` のみに設定 |
| 4 | `case_invitations` の RLS・部分ユニーク・`sent_at` を追加。送信は必ず再発行を伴う仕様に確定 |
| 5 | `user_profiles_admin_write` の `with check` を U02 の規則へ整合 |
| 6 | `memo` を列レベル権限で剥奪し `get_couple_memo()` 経由に変更。書き込みを staff 限定へ |
| 7 | `submission_type` を4値へ統一。`submission_format='none'` の完了手順を定義 |
| 8 | パスワード最小長を12文字に統一。`/api/auth/complete-invite` を新設 |

### 設計 — 第13章 Phase 1 合意必須事項の決定

**13-1「開発チーム決定（2026年8月28日）」** を新設し、8件を差し戻し影響範囲つきで確定。
ワンタイム認証の採用／招待トークン有効期限（14日・30日）／招待とメールの結び付き／
個人情報の暗号化（AES-256-GCM＋HMAC）／自動削除（180日）／ゲスト情報の同意／
セッション失効（`status` によるDB層遮断）／認証メールの送信ドメイン。

### 実装 — Phase 1

コア導線（案件登録 → 招待 → 初回登録 → 提出 → 確認）を実装。全34ルート。

| 区分 | 内容 |
| --- | --- |
| DB | 26テーブル・RLS共通関数・全ポリシー・インデックス・seed（マイグレーション11本） |
| 認証 | P01〜P04。couple=マジックリンク＋6桁コード、staff=メール＋パスワード |
| 案件 | K01〜K05。case_code 採番・宿題一括割当・タイムライン逆算・招待の発行/再発行/送信 |
| マイページ | M01〜M04＋案件詳細。提出（text/select/file/none）・一時保存・再提出 |
| プランナー | D01・D02・D04。提出物確認・フォロー記録 |
| 管理 | T01〜T03・U01〜U04 |
| 検証 | **175件**（RLSテスト82件＋ユニットテスト93件）。lint・型チェック・ビルドとも通過 |

### 実装レビューで検出・修正した欠陥

観点別4並列レビュー＋high 指摘の反証検証を行い、確認された欠陥を修正した。
**いずれも「設計の方針は正しいが、それを成立させる権限・条件がポリシー集合に無い」型**で、
PGlite 上のRLSテストで再発を防いでいる。

| 深刻度 | 欠陥 | 対応 |
| --- | --- | --- |
| high | 相手が一時保存すると、もう一方が二度と提出できない（409 が恒久化）。6-7 の中心導線が塞がっていた | 提出の判定単位を利用者から案件へ。`latest_submission_for_task()` / `claim_latest_submission()` を追加 |
| high | `task_submissions_review_planner` の WITH CHECK が値域だけを見ており、couple が自分の提出を confirmed にできた（権限昇格） | WITH CHECK にロール条件を追加 |
| high | 不備指摘からの再提出が部分ユニークで失敗していた | `demote_latest_submission()` を追加 |
| medium | 提出済み・確認済みの宿題に waived を付けて解除すると not_started へ巻き戻り、確認実績が消える | 付与は not_started のみ・解除は waived のみに制限（DB側で拒否＋画面でボタン非表示） |
| medium | P01 の `next` パラメータがバックスラッシュを素通しし、オープンリダイレクトが成立 | 制御文字とバックスラッシュを弾く判定へ |
| medium | 招待メールの送信失敗が 500 になり、その応答でしか返らない平文の招待URLを失う | 送信を1実装へ統合し例外を畳む |
| medium | K02 の couple 向け画面が無い（Phase 1 機能2-3 の欠落） | マイページ内に新設 |
| medium | 一覧が50件で頭打ちになり51件目以降へ到達できない（T01/T03/U01/D04） | K01・M02 と同じページングへ統一 |
| medium | `communication_logs` に insert ポリシーが無く 6-7 の自動記録が書けない | `log_communication()` を追加 |
| low | 挙式日の「過去日付不可」が UTC 基準で、JST の 0〜9時に前日が通る | 基準日を JST の暦日へ |
| low | M01 の「次にやること」と「未提出件数」が別の母集合で矛盾 | 未提出判定に一本化 |
| low | 復号・日付整形が複数方式に分裂（鍵不一致時に画面ごとに 500 と表示が割れる） | `readPii()` / `format.ts` に集約 |

## 2. 残タスク

### Phase 1 の仕上げ

- ⬜ **実環境での通し確認**（#14） — Supabase プロジェクトを作成し、マイグレーションと seed を適用して
  E2E（`e2e/`、Playwright）を通す。以下は接続情報が無いと検証できない。
  - Supabase Auth のメールテンプレートに `{{ .Token }}` を追加（6-3-1 の「リンクと6桁コードの両方」はテンプレート側の設定でしか満たせない）
  - Custom SMTP に Resend を設定、Leaked Password Protection を有効化
  - Storage bucket `case-files` と `storage.objects` のポリシー適用（マイグレーションは `storage` スキーマがあるときだけ実行する形で用意済み）
- ⬜ 認証メール用の独自ドメイン取得と SPF／DKIM 設定（13-1） → #17
- ⬜ ワンタイムコードの「検証失敗5回で失効」（5-3 の初期値のうちこれだけ未実装。`auth_rate_limits.locked_until` は定義済み） → #15
- ⬜ 提出・確認の単一トランザクション化（6-7 に Phase 2 の課題として明記済み） → #16
- ⬜ 5人並列実装で残った重複と規約の不統一（K01／U01 の画面とAPIの二重実装、
  403/404 → P04 遷移の未統一、`requireAppUser` の二重認証確認） → #18

### Phase 2 / Phase 3

- ⬜ Phase 2（LINE起点導線・リスク可視化・通知・打ち合わせ前準備シート・system_admin画面）
- ⬜ Phase 3（AI補助 9-1〜9-6）／Phase 3拡張（9-7〜9-11）
- ⬜ 第13章の残りの合意必須4件（AIジョブログ保持・LLMモデル選定・FAQ一次回答の採用可否・
  venue_knowledge の投入担当）。いずれも Phase 3／Phase 3拡張の着手前が期限

## 3. 実装で踏みやすい罠（新しく触る人向け）

- **RLS の共通関数は再帰回避のために必須**。ポリシー式から `user_profiles` / `couple_profiles` /
  `wedding_cases` を直接参照すると 42P17 で主要画面が落ちる。
- **permissive ポリシーの WITH CHECK は OR 結合される**。値域だけの WITH CHECK は
  別ポリシーの USING と組み合わさって権限昇格になる。USING と WITH CHECK の両方に同じロール条件を置く。
- **`couple_profiles` は `select *` が 42501**。`memo` を列レベル権限で剥奪しているため
  `COUPLE_PROFILE_COLUMNS` を使う。
- **提出は案件単位**。新郎新婦は同じ宿題に1つの提出を持つ。提出者一致を条件に加えない。
- **平文の招待トークンは保存しない**。送信・URL再表示は必ず再発行を伴う。
- **Service Role Key は使用範囲表（6-3-5 表6-4）にある用途のみ**。範囲外の参照は ESLint が落とす。

## 4. レビュー指摘 全55件（v1.1 で反映済み・参照用）

重要 25 / 軽微 30。検証で棄却された27件は除外済み。詳細は `docs/BridalHub_基本設計書_レビュー結果.md`。
rank 1〜10 の検証結果は本書 1章のとおり。rank 11 以降は v1.1 の反映内容をもって解消とする。
